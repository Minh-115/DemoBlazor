name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

jobs:
  ai-review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Get PR Diff
        run: |
          # Lấy diff giữa nhánh base (ví dụ: master/develop) và nhánh hiện tại
          gh api repos/${{ github.repository }}/pulls/${{ github.event.pull_request.number }} \
          -H "Accept: application/vnd.github.v3.diff" > diff.patch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run AI Review
        id: ai-review
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          # Đọc diff và giới hạn kích thước để tránh vượt quá input model
          DIFF=$(cat diff.patch | head -c 15000)

          # Gọi OpenAI API để review code
          REVIEW=$(curl -s https://api.openai.com/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -d "{
              \"model\": \"gpt-4o-mini\",
              \"messages\": [
                {\"role\": \"system\", \"content\": \"Bạn là code reviewer chuyên nghiệp, hãy review diff code và nêu nhận xét rõ ràng, ngắn gọn\"},
                {\"role\": \"user\", \"content\": \"$DIFF\"}
              ]
            }" | jq -r '.choices[0].message.content')

          # Lưu output để step sau có thể dùng
          echo "review<<EOF" >> $GITHUB_OUTPUT
          echo "$REVIEW" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Post Review Comment
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          header: ai-review
          message: |
            🤖 **AI Code Review Report**
            ---
            ```
            ${{ steps.ai-review.outputs.review }}
            ```
            _Generated by OpenAI API_
