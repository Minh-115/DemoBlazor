name: AI Code Review

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

permissions:
  contents: read
  pull-requests: write

jobs:
  ai-review:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3
        fetch-depth: 0
        ref: ${{ github.event.pull_request.head.sha }}

      - name: Get PR Diff
        run: |
          git diff origin/${{ github.base_ref }}...origin/${{ github.head_ref }} > diff.patch
          echo "=== DIFF SIZE ==="
          wc -c diff.patch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Run AI Review
        id: ai-review
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          if [ ! -s diff.patch ]; then
            echo "review<<EOF" >> $GITHUB_OUTPUT
            echo "✅ Không có thay đổi đáng kể để review." >> $GITHUB_OUTPUT
            echo "EOF" >> $GITHUB_OUTPUT
            exit 0
          fi

          DIFF=$(head -n 500 diff.patch)

          REVIEW=$(curl -s https://api.openai.com/v1/chat/completions \
            -H "Content-Type: application/json" \
            -H "Authorization: Bearer $OPENAI_API_KEY" \
            -d "{
              \"model\": \"gpt-4o-mini\",
              \"messages\": [
                {\"role\": \"system\", \"content\": \"Bạn là code reviewer chuyên nghiệp. Hãy phân tích phần diff sau, đưa ra: 1. Điểm tốt 2. Điểm cần cải thiện 3. Đề xuất cụ thể. Ngắn gọn, súc tích.\"},
                {\"role\": \"user\", \"content\": \"$DIFF\"}
              ]
            }" | jq -r '.choices[0].message.content // "⚠️ AI không đưa ra nhận xét."')

          echo "review<<EOF" >> $GITHUB_OUTPUT
          echo "$REVIEW" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: Post Review Comment
        uses: marocchino/sticky-pull-request-comment@v2
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          header: ai-review
          message: |
            🤖 **AI Code Review Report**
            ${{ steps.ai-review.outputs.review }}
